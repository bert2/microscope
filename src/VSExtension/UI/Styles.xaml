<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:vs="clr-namespace:Microsoft.VisualStudio.CodeSense.Controls;assembly=Microsoft.VisualStudio.CodeSense.Common">
  <ResourceDictionary.MergedDictionaries>
    <x:Static Member="vs:SharedResources.Default" />
  </ResourceDictionary.MergedDictionaries>

  <Style BasedOn="{StaticResource {x:Static vs:CodeSenseResources.ListViewStyleKey}}" TargetType="{x:Type ListView}">
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
  </Style>

  <Style BasedOn="{StaticResource {x:Static vs:CodeSenseResources.ListViewItemStyleKey}}" TargetType="{x:Type ListViewItem}">
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
  </Style>

  <Style BasedOn="{StaticResource {x:Static vs:CodeSenseResources.TreeViewStyleKey}}" TargetType="{x:Type TreeView}">
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
  </Style>

  <Style BasedOn="{StaticResource {x:Static vs:CodeSenseResources.TreeViewItemStyleKey}}" TargetType="{x:Type TreeViewItem}" />

  <Style
    x:Key="IndentedTreeViewItemStyle"
    BasedOn="{StaticResource {x:Static vs:CodeSenseResources.TreeViewItemStyleKey}}"
    TargetType="{x:Type TreeViewItem}">
    <Setter Property="Padding" Value="16,0,0,0" />
  </Style>
</ResourceDictionary>
